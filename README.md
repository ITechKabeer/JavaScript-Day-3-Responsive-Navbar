# JavaScript-Day-3-Responsive-Navbar
🧩 A modern and fully responsive navigation bar built using pure JavaScript DOM manipulation and CSS gradients. The navbar dynamically generates logo, links, and login buttons with hover animations, text shadows, and smooth transitions — all without any static HTML.
